---
description: Always format and lint code using Biome
globs: app/**
---

Always use Biome for formatting and linting. Run `bunx biome check --write` to format and lint code. Never use ESLint or Prettier directly.
