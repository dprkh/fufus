---
description: Always use bun as the package manager
globs: app/**
---

Always use `bun` as the package manager. Use `bun install`, `bun add`, `bun remove`, `bun run`, etc. Never use npm, yarn, or pnpm.
